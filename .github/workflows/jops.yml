on: 
  workflow_dispatch:


jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "job_1 ${{ github.job }}" 

  job_2:
    runs-on: ubuntu-latest
    needs: job_1
    steps:
      - run: echo "job_2 ${{ github.job }}" 

  jop_2:
    runs-on: ubuntu-latest
    needs: job_1
    steps:
      - run: echo "job_3 ${{ github.job }}"

  job_4:
    runs-on: ubuntu-latest
    needs: [job_1, job_2]
    steps:
      - run: echo "job_4 ${{ github.job }}" 
